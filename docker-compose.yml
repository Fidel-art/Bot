services:
  backend:
    build:
      context: ./smc_bot
      dockerfile: backend/Dockerfile
    container_name: smc_bot_backend
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=your-secret-key-change-in-production
      - DATABASE_PATH=/app/data/bot_dashboard.db
      - LOG_LEVEL=INFO
      - MT5_BRIDGE_URL=http://host.docker.internal:8765
    volumes:
      - ./smc_bot/data:/app/data
      - ./smc_bot/logs:/app/logs
      - ./smc_bot/backend:/app/backend
      - ./smc_bot/mt5_proxy:/app/mt5_proxy
    networks:
      - smc_network
    restart: unless-stopped
    command: uvicorn backend.api:app --host 0.0.0.0 --port 8000
    depends_on:
      - database
    extra_hosts:
      - "host.docker.internal:host-gateway"

  frontend:
    build:
      context: ./smc_bot/frontend
      dockerfile: Dockerfile
    container_name: smc_bot_frontend
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=http://backend:8000
      - NODE_ENV=development
    volumes:
      - ./smc_bot/frontend/src:/app/src
      - ./smc_bot/frontend/public:/app/public
    networks:
      - smc_network
    restart: unless-stopped
    command: npm run dev -- --host 0.0.0.0
    depends_on:
      - backend

  database:
    image: alpine:latest
    container_name: smc_bot_database
    volumes:
      - ./smc_bot/data:/data
      - ./smc_bot/database:/database
    networks:
      - smc_network
    command: tail -f /dev/null
    restart: unless-stopped

  bot:
    build:
      context: ./smc_bot
      dockerfile: Dockerfile.bot
    container_name: smc_bot_core
    environment:
      - MT5_BRIDGE_URL=http://host.docker.internal:8765
      - MT5_LOGIN=${MT5_LOGIN:-}
      - MT5_PASSWORD=${MT5_PASSWORD:-}
      - MT5_SERVER=${MT5_SERVER:-}
    volumes:
      - ./smc_bot/data:/app/data
      - ./smc_bot/logs:/app/logs
      - ./smc_bot/config:/app/config
      - ./smc_bot/mt5_proxy:/app/mt5_proxy
    networks:
      - smc_network
    restart: unless-stopped
    command: python bot_run.py
    depends_on:
      - database
    extra_hosts:
      - "host.docker.internal:host-gateway"

networks:
  smc_network:
    driver: bridge

volumes:
  data:
    driver: local
  logs:
    driver: local
